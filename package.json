{
  "name": "queueway-monorepo",
  "version": "0.0.1",
  "description": "Queueway - Zero-Config Job Queue",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "dev": "lerna run dev --parallel",
    "lint": "lerna run lint"
  },
  "keywords": [
    "queue",
    "job",
    "message-broker"
  ],
  "author": "Syed Farrukh Ahmed",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^8.0.0"
  }
}
