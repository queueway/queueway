node_modules/
dist/
.env
.env.local
*.log
.DS_Store
coverage/
lerna-debug.log
.turbo/
.next/
*.db
.queueway/
packages/dashboard/out/
packages/core/public/
packages/core/README.md
packages/core/LICENSE

# Generated by `queueway init` in a user's project — contains generated
# database credentials, so it must never be committed.
docker-compose.queueway.yml

# Local test artifacts from running the CLI inside this repo
queueway.config.js
queueway.jobs.js
queueway.db
